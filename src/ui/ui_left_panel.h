#pragma once

struct AppState;

/**
 * @brief  Render the left panel: resource list, batch-convert, and compose tabs.
 * @param  state  Application state.
 */
void render_left_panel(AppState& state);
