#pragma once

struct AppState;

/**
 * @brief  Render the right panel: thumbnail grid and single-image preview/save toolbar.
 * @param  state  Application state.
 */
void render_right_panel(AppState& state);
